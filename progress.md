Original prompt: ускорь дополнительные ходы, при этом не создавай новых багов

- 2026-03-10: Стартовый модал уровня теперь показывает реальную цель выбранного уровня до входа в матч (`src/App.tsx`, `src/components/LevelStartModal.tsx`) вместо пустого `Press Play to start`; добавлен краткий pace-hint по лимиту ходов/времени.
- 2026-03-10: Ускорен flow матча без изменения правил (`src/hooks/useGame.ts`, `src/App.tsx`): сокращены паузы reveal/remove/gravity/cascade, ускорен откат неудачного свапа и уменьшены задержки после `Goal Complete`/перед `Level Up`, чтобы игра ощущалась живее.
- 2026-03-10: `npm run build` успешен после правок старта уровня и темпа. Playwright-прогон по skill не выполнялся в этом заходе.
- 2026-03-10: Экран поражения теперь предлагает продолжить матч за монеты (`src/components/GameOverMenu.tsx`, `src/App.tsx`): для move-level даёт `+5 moves`, для time-level `+30s`; при нехватке монет кнопка ведёт в магазин.
- 2026-03-10: Локальная browser-проверка через Playwright подтвердила оба сценария `game over -> continue`: move-режим и time-режим возвращают игрока в матч и списывают монеты. Артефакты: `output/gameover-moves.png`, `output/gameover-moves-continued.png`, `output/gameover-time.png`, `output/gameover-time-continued.png`.
- 2026-03-10: Для видеоматериалов в `../Videos` убран неудачный `zoompan` из `render_reels.py`; текущая автоматическая нарезка больше не делает ползущий fake-zoom в начале роликов.
- 2026-03-10: Подготовлены новые hook-фразы для 8 рилсов в `../Videos/reels_hook_rewrites.md` и storyboard трейлера на 70-85 секунд в `../Videos/trailer_storyboard.md`; направление: меньше generic suspense, больше конкретного визуального payoff.
- 2026-03-10: Рилсы в `../Videos/output/reels_final` пересобраны с новыми hook-текстами и без zoom-crawl.
- 2026-03-10: Добавлены браузерные утилиты захвата трейлерных материалов: `scripts/capture_trailer_shots.cjs` и `scripts/capture_trailer_clips.cjs`.
- 2026-03-10: Автоматически снят shot pack для трейлера в `output/trailer-shots` (landing, tutorial/start, roadmap, level-start modal, daily, leaderboard) и короткий b-roll pack в `output/trailer-clips` (landing->tutorial, roadmap->level-start, daily, leaderboard).
- TODO: Для полноценного трейлера всё ещё нужно вручную записать авторские gameplay-моменты, которых нельзя гарантированно воспроизвести браузерной автоматизацией: сильные каскады, boss hit, clutch/continue, level complete/goal complete.

- 2026-03-04: Ускорена только финальная анимация конвертации оставшихся ходов в `runVictoryMoveBonus`; игровая логика начисления/списания ходов не менялась.
- 2026-03-04: Сокращены локальные тайминги взрыва, удаления, падения и автосовпадений для бонусных добивок уровня, чтобы уменьшить риск регрессий вне этого сценария.
- 2026-03-04: `npm run build` прошёл успешно. Автоматический Playwright-прогон из skill не выполнен: в проекте отсутствует пакет `playwright`, поэтому клиент `web_game_playwright_client.js` не стартует.
- 2026-03-04: Платёжный возврат после Robokassa теперь не считает оплату мгновенно успешной вслепую: фронтенд опрашивает статус заказа по `orderId` и ждёт подтверждения `credited`, затем обновляет баланс.
- 2026-03-04: Добавлены funnel-события для `landing_view`, ранней готовности кошелька, checkout, возврата из оплаты и подтверждённого/отложенного зачисления; также `level_start` теперь отправляется и для первого уровня.
- 2026-03-04: `npm run build` повторно прошёл после правок оплаты и аналитики.
- 2026-03-04: Исправлена конфигурация воронки в аналитике: `trackEvent` теперь лениво инициализирует провайдеры при первом событии, а ключевые шаги дополнительно отправляются единым событием `funnel_step` с `funnel_name`, `funnel_step`, `funnel_index`.
- 2026-03-04: `npm run build` прошёл после правки унифицированной воронки.
- TODO: Прогнать ручной сценарий оплаты на реальном сервере: checkout -> возврат -> webhook -> подтверждённое зачисление по `/api/payments/order-status`.
- TODO: Прогнать визуальную проверку конца уровня и убедиться, что темп выглядит быстрее, но счёт, цели и обнуление оставшихся ходов синхронны.
- 2026-03-06: Аналитика переведена на GTM-контейнер (`VITE_GTM_CONTAINER_ID`, по умолчанию `GTM-54KD4D8H`): инициализация через `gtm.js`, события отправляются в `window.dataLayer` как `{ event, ...payload }`; direct `gtag` оставлен только как fallback при отключенном GTM.
- 2026-03-06: Для DebugView добавлена авто-активация `debug_mode` при `ga_debug=1/true`, `gtm_debug`, `gtm_preview` и на localhost.
- 2026-03-06: Сборка `npm run build` успешна после правок аналитики.
- 2026-03-06: Локальная браузерная проверка (`output/web-game/analytics-check.json`) подтвердила события в dataLayer: `ga_debug_probe`, `landing_view`, `session_start`, `level_start`, `landing_play_click`, `funnel_step`.
- 2026-03-06: Фронтенд задеплоен вручную через `pscp/plink` (первичный `deploy-per.ps1` падал на `scp: Connection closed` в текущем окружении).
- 2026-03-06: Прод-проверка (`output/web-game/analytics-prod-check.json`) подтвердила dataLayer-события на `https://nebulaclash.com` после клика "Играть".
- TODO: Проверить и при необходимости расширить CSP `script-src` для внешнего CMP-скрипта `https://cdn.consentmanager.net/...` (в консоли прода фиксируется блокировка CSP, на отправку GTM-событий не повлияло).
- 2026-03-06: Backend wallet/payments переведён с JSON-файла на SQLite (`data/wallet-state.sqlite`) в `server/index.mjs`; добавлены SQL-таблицы `wallets` и `orders`, транзакционные обновления баланса/заказов и миграция legacy-данных из `data/wallet-state.json` при первом запуске.
- 2026-03-06: Проверен локальный smoke API (session -> wallet -> spend -> create-invoice -> order-status): сценарий проходит, статус нового заказа `created`, баланс корректно списывается.
- 2026-03-06: Проверена идемпотентность webhook Robokassa Result URL: повторный вызов с теми же параметрами не делает повторного начисления; заказ получает `credited`, баланс увеличивается ровно на один пакет.
- TODO: Выкатить backend-обновление `server/index.mjs` на прод-сервер и прогнать боевой платежный сценарий end-to-end (checkout -> result webhook -> order-status credited).
- 2026-03-06: Выявлен prod-блокер для выката SQLite-версии backend: на сервере Node.js `v10.24.0`, а реализация использует `node:sqlite` (нужен современный Node). На сервере есть `sqlite3` CLI `3.26.0`, можно сделать fallback-реализацию под Node 10 или обновить Node на сервере и выкатить текущую версию.
- 2026-03-06: На прод-сервер установлен Node.js `v22.13.1` в user-space (`/var/www/u3426655/data/opt/node-v22.13.1-linux-x64`) и backend startup-скрипты переключены на этот runtime.
- 2026-03-06: Backend `server/index.mjs` выкачен на прод и успешно запущен под Node 22; процесс: `/var/www/u3426655/data/opt/node-v22.13.1-linux-x64/bin/node server/index.mjs`.
- 2026-03-06: Прод-smoke через `https://nebulaclash.com/api/*` успешен: `health.ok=true`, `session/init ok=true`, чтение wallet работает, `create-invoice` создаёт заказ, `order-status` возвращает `created`.
- 2026-03-06: На проде создана и используется SQLite state-база: `data/wallet-state.sqlite` (+`-wal`/`-shm`), legacy `wallet-state.json` сохранён как источник для миграции/бэкапа.
- 2026-03-06: Добавлен A/B эксперимент `shop_timing_v1` в `src/App.tsx`: 50/50 assignment (`a`/`b`) с сохранением варианта в `localStorage` (`match3_exp_shop_timing_v1_variant`), событие `experiment_assigned`, ивент `shop_prompt_shown` с `source`.
- 2026-03-06: Для варианта `b` реализован автопоказ магазина после первого `level_complete` на уровне 1 (однократно на пользователя, ключ `match3_exp_shop_timing_v1_auto_shown`), для ручного открытия источник `source=manual_button`.
- 2026-03-06: Выполнен визуальный редизайн фонов для лендинга/игры/roadmap: обновлены слои `CosmicBackdrop` (`src/components/CosmicArtwork.tsx`), фон и стеклянная глубина игрового поля (`src/components/GameBoard.tsx`), фон контейнера и атмосферные оверлеи roadmap (`src/components/SpaceRoadmap.tsx`), а также главный фон игрового экрана (`src/App.tsx`).
- 2026-03-06: `npm run build` успешно после правок визуала (первый запуск в sandbox падал на `spawn EPERM`, повторный запуск вне sandbox прошёл).
- 2026-03-06: Прогнан Playwright-клиент skill (временная локальная копия скрипта, после прогона удалена) после редизайна; получены артефакты в `output/web-game` и `output/web-game/bg-roadmap`. Визуально проверены скриншоты:
  - лендинг: `output/web-game/shot-0.png`
  - игровой экран с туториалом: `output/web-game/bg-roadmap/shot-0.png`
  - roadmap на `/play`: `output/web-game/bg-roadmap-map.png`
- 2026-03-06: В console-errors Playwright на локальном dev сохраняется `Failed to load resource: ... 500 (Internal Server Error)` (в `errors-0.json`), вероятно связано с отсутствующим локальным backend API в dev-окружении; на редизайн фонов не влияет.
- 2026-03-07: По запросу убран фон игрового поля в src/components/GameBoard.tsx: удалены декоративные gradient/grid overlay-слои и фон контейнера поля (bg-* и backdrop-blur), чтобы снизить риск поломки UI на телефонах.
- 2026-03-07: Немного ускорена финальная конвертация оставшихся ходов в runVictoryMoveBonus (src/hooks/useGame.ts): VICTORY_BONUS_EXPLOSION_MS 140->120, VICTORY_BONUS_REMOVE_MS 80->70, VICTORY_BONUS_GRAVITY_MS 110->95, VICTORY_BONUS_AUTO_MATCH_MS 120->105. Логика списания ходов/времени не изменялась.
- 2026-03-08: Выполнен редизайн игрового UI: обновлены космические градиенты и атмосферные слои (src/components/CosmicArtwork.tsx, src/index.css), усилены контейнер и карточки главного экрана, переработаны кнопки паузы/магазина, рамка поля и нижняя панель с бустерами и иконками (src/App.tsx). Проверка: npm run build успешно.
- 2026-03-08: Улучшены монеты и UX: увеличен и переработан виджет баланса монет в игровом HUD, перерисован CoinGlyph (многослойный metallic-стиль), добавлены рабочие быстрые действия в нижней панели (экстренный буст ходов/времени за монеты, быстрый вход в магазин и гайд). Проверка: npm run build успешно.
- 2026-03-08: Добавлен верхний отступ для плашки уровня (StarProgress + контейнер в App), чтобы она не прилипала к верхнему краю. Дополнительно оптимизирован StarProgress для lowPerfMode: отключён star-burst, упрощены анимации и blur прогресс-бара. Общий lowPerf-режим в App уже учитывает prefers-reduced-motion, deviceMemory/hardwareConcurrency, coarse pointer и компактный viewport. Проверка: npm run build успешно.
- 2026-03-08: Добавлены новые игровые мета-функции. Backend (server/index.mjs): новые таблицы reward_claims, daily_rewards, leaderboard_profiles; API: /api/rewards/level-complete, /api/rewards/daily-status, /api/rewards/daily-claim, /api/leaderboard/submit, /api/leaderboard/top. Frontend: useWallet расширен методами для rewards/leaderboard; в App добавлены награды за завершение уровня, ежедневные награды, рейтинг игроков (модалка Top), и кнопка «Поделиться» с Web Share API + fallback на clipboard. Сборка npm run build успешна.
- 2026-03-08: По UX-правке ежедневные награды и рейтинг перенесены в фиксированный верхний overlay экрана roadmap (поверх карты), а кнопка «Поделиться» добавлена в настройки roadmap и в pause-настройки игрового интерфейса. Из игрового HUD убраны дублирующие daily/top/share-кнопки. Проверка: npm run build успешно.
- 2026-03-08: Daily/Top кнопки перемещены непосредственно внутрь поля roadmap (внутрь карточки карты), закреплены поверх интерфейса карты и не вынесены вне поля; внешний overlay над картой удалён. Проверка: npm run build успешно.
- 2026-03-08: Исправлена кликабельность кнопок Daily/Top на roadmap: убран блокирующий pointer-events на контейнере, кнопки сделаны квадратными 20x20, расположены слева столбиком и зафиксированы поверх карты, при этом скролл работает для остального roadmap-контента. Проверка: npm run build успешно.
- 2026-03-08: Исправлен регресс скролла roadmap после закрепления кнопок: контейнер скролла переведён на `h-full` внутри `flex-1`-обёртки, скролл карты восстановлен. Кнопки Daily/Top увеличены до 36x36 для лучшей видимости, остаются закреплёнными слева столбиком поверх карты. Проверка: npm run build успешно.
- 2026-03-08: Дополнительно исправлен flex-layout roadmap для мобильного скролла: у обёртки карты добавлен `min-h-0` (`relative flex-1 min-h-0`), чтобы `overflow-y-auto` не терял рабочую высоту в колонке. Проверка: `npm run build` успешно + Playwright smoke на `http://127.0.0.1:4174/play` подтвердил прокрутку (`scrollTop 6434 -> 5234`, delta `-1200`) и скриншот `output/roadmap-scroll-check.png`.
- 2026-03-08: Увеличены кнопки Daily/Top на roadmap до 48x48, усилен слой кликабельности (`z-[70]`, `isolate`, `pointer-events-auto`) и добавлен `stopPropagation` на pointer/touch для исключения перехвата скроллером карты. Проверка: `npm run build` успешно.
- 2026-03-08: Исправлено открытие интерфейсов Daily/Top по тапу на roadmap: модалки Daily Reward и Player Ranking вынесены в map-ветку рядом с `SpaceRoadmap` (вне `AnimatePresence`), на кнопки добавлены нативные tap-listeners через `ref` (`pointerup`/`touchend`) + `onClick` fallback. Проверка: `npm run build` успешно; Playwright check на preview `http://127.0.0.1:4174/play` дал `rank=1`, `daily=1` (скриншот `output/roadmap-widget-open-check4.png`).
- 2026-03-15: Усилен верх воронки на лендинге (`src/components/MarketingLanding.tsx`): заголовок и body теперь прямо продают browser-first вход без установки, добавлены proof-бейджи и более жёсткий CTA `Play the first level now` / `Запустить первый уровень`.
- 2026-03-15: Ускорен first payoff для новых игроков (`src/logic/levelProgress.ts`, `src/App.tsx`): первые onboarding-уровни упрощены по целям и лимитам, а вводный tutorial-step убран, поэтому после CTA игрок попадает сразу в шаг `Собери первый матч` вместо общего интро-экрана.
- 2026-03-15: Расширен эксперимент тайминга магазина до `shop_timing_v2` (`src/App.tsx`): варианты `a=manual only`, `b=auto after level 1 complete`, `c=auto after level 1 fail`; события `shop_prompt_shown` и `shop_open` теперь позволяют сравнить ранний успех против раннего фейла.
- 2026-03-15: Проверка: `npm run build` успешен вне sandbox после правок воронки/онбординга/shop timing. Визуальная browser-проверка через локальный Playwright fallback дала артефакты `output/web-game/manual-check-2/landing.png` и `output/web-game/manual-check-2/entry.png`; визуально лендинг стал жёстче по promise, а первый экран после CTA сразу ведёт к первому матчу.
- 2026-03-15: Исправлена мобильная адаптация лендинга (`src/components/MarketingLanding.tsx`): layout теперь переключается по реальной комбинации высоты и ширины viewport, а не по `min-width`. Это убрало переполнение на `360x640` и обрезание CTA на пограничном `390x844`, при этом `430x932` остаётся более полной версией с дополнительным proof-блоком. Артефакты финальной проверки: `output/playwright/mobile-landing-audit-2026-03-15/final-360x640.png`, `final-390x844.png`, `final-430x932.png`.
- 2026-03-15: Усилен первый платёжный оффер (`src/components/ShopModal.tsx`, `src/App.tsx`): магазин теперь получает контекст открытия (`manual`, `momentum`, `recovery`) и меняет hero-copy под ситуацию игрока; средний пак выделен как starter pick, а карточки паков теперь объясняют практическую ценность в количестве возможных continue/boost-покупок.
- 2026-03-15: Подтянут retention-слой (`src/App.tsx`, `src/components/SpaceRoadmap.tsx`): добавлены локально-персистентные `bestScore` и `weekly loop` state, видимый на roadmap блок с `return streak`, `beat this score` и `weekly loop`, а challenge target автоматически подтягивается из leaderboard snapshot или строится от локального best score.
- 2026-03-15: Weekly loop прогресс обновляется на daily claim и level complete; при закрытии challenge игрок получает явный in-app notice. Сборка `npm run build` успешна после правок оффера и retention.
- 2026-03-15: Доделана reward-логика daily и прогрессии (`server/index.mjs`, `src/App.tsx`, `src/hooks/useWallet.ts`): daily rewards теперь идут по 30-дневному календарю с milestone-бонусами на 7 / 14 / 21 / 30 дне, backend хранит `total_claims`, а фронтенд показывает весь цикл в модалке. За завершение уровней добавлены milestone-награды за количество закрытых уровней (`3 / 5 / 10 / 15 / 20 / 30`), чтобы мета-прогресс не упирался только в единичный clear reward.
- 2026-03-15: Исправлен пустой рейтинг (`server/index.mjs`, `src/App.tsx`): если живых записей leaderboard пока мало, API заполняет top-list fallback-пилотами с реальными-looking score/level значениями; модалка рейтинга одновременно показывает `Beat this score`, лучший локальный счёт игрока и состояние weekly challenge.
- 2026-03-15: `return streak`, `beat this score` и `weekly loop` встроены обратно внутрь самой карты roadmap (`src/components/SpaceRoadmap.tsx`) как отдельные embedded map-buttons рядом с daily/ranking; крупный верхний блок удалён, поэтому roadmap снова занимает полную высоту страницы. Локальная проверка через Playwright на `390x844` подтвердила отсутствие лишнего вертикального блока и наличие всех пяти map-actions.
- 2026-03-15: Закрыт следующий product cycle по retention + monetization (`src/App.tsx`, `src/hooks/useWallet.ts`, `src/hooks/useGame.ts`, `src/components/LevelStartModal.tsx`, `server/index.mjs`): перед стартом уровня добавлены платные `run modifiers` (`start bomb`, `start lightning`, `boss shield`, `trash cleaner`), в daily reward modal встроен `daily missions / quest board` с 3 миссиями и claim-наградами, а leaderboard усилен блоками `next rival`, `weekly tier` и `rank chest`.
- TODO: Прогнать отдельный browser-сценарий именно для варианта `shop_timing_v2=c` на живом dev/preview с рабочим API proxy или добавить безопасный debug-hook только для локальной валидации auto-open после раннего fail.
